
- execute .xor file to bootstrap it using a disposable interpreter

For some very strange reason, when i feed any of the generated .py to itself,
the produced encrypted data doesn't EXACTLY match the one in the .xor file.
(Despite the python files being the same bit by bit)
